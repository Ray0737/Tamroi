# ── Personal / unrelated files ───────────────────────
*.docx
*.xlsx
*.pptx
*.pdf

# ── Supabase / API credentials — NEVER commit these ──
js/env.js
.env
.env.local
.env.production
.env.production.local
.env.*.local
*.env

# ── OS / editor artifacts ─────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ── VS Code (only settings.json might have secrets) ───
.vscode/settings.json
.vscode/*.code-workspace

# ── Node (added in case npm/node is ever introduced) ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# ── Build outputs ─────────────────────────────────────
dist/
build/
.vercel/

# ── Logs ──────────────────────────────────────────────
*.log
logs/
